# 隧道三维数字孪生平台 · 使用说明

> 本文件对应交付物：`隧道三维数字孪生平台.html` ＋ `vendor/three.min.js`
> 所属作品：隧雷智检——基于探地雷达的隧道隐蔽病害智能检测与分析系统
> 对应技术路线第 ⑤ 步"平台演示"（数字孪生平台：管片级三维模型 + 病害标注 + 时序对比 + 报警推送 + 报告自动生成）

---

## 一、快速开始

### 1. 运行环境

| 项目 | 要求 |
|---|---|
| 浏览器 | Chrome / Edge / Firefox 等支持 WebGL 的现代浏览器（推荐 Chrome 或 Edge 最新版） |
| 网络 | **无需联网**（three.js 已本地内置） |
| 服务器 | **无需服务器**，双击 HTML 即可运行 |
| 显卡 | 集成显卡即可流畅运行；建议 1920×1080 及以上分辨率 |

### 2. 启动方式

**在线访问（免安装）**：<https://llllllwwww.github.io/platform/>

**本地运行**：双击 `隧道三维数字孪生平台.html`（或站点入口 `index.html`），或在浏览器中打开该文件。

> ⚠️ **重要**：`vendor` 文件夹必须与 HTML 文件保持同级目录，不可单独移动 HTML。
> 若移动了文件位置，请保持两者相对关系不变：
> ```
> 三维孪生平台/
> ├── index.html                 （站点入口，自动跳转到主页面）
> ├── 隧道三维数字孪生平台.html
> └── vendor/
>     └── three.min.js
> ```

### 3. 异常处理

| 现象 | 原因与处理 |
|---|---|
| 页面提示"三维引擎 three.js 未能加载" | `vendor/three.min.js` 缺失或路径不对，确认目录结构 |
| 页面提示"当前浏览器无法创建 WebGL 上下文" | 浏览器 WebGL 被禁用或显卡驱动异常，换用 Chrome/Edge，或在设置中开启硬件加速 |
| 画面全黑但侧栏正常 | 显卡驱动兼容问题，尝试刷新页面或更新浏览器 |

---

## 二、鼠标与视角操作

### 1. 基础操作（三键交互）

| 操作 | 效果 |
|---|---|
| **左键拖动** | 360° 任意翻转隧道（球面轨道旋转） |
| **滚轮上/下** | 放大 / 缩小（范围 0.8 m ~ 160 m） |
| **右键拖动** | 平移视图（改变观察中心） |
| **左键单击病害体** | 选中该病害，右侧弹出量化详情 |
| **鼠标悬停病害体** | 弹出浮动提示（类型、环号、环向角、d_eq、埋深、置信度、等级） |
| **触摸屏双指捏合** | 缩放 |

> 提示：拖动过程中光标为"抓手"状；单击判定容差为 4 像素，轻微手抖不会误触发选中。

### 2. 视角预设（顶栏右侧：6 个视角按钮 + 2 个漫游模式 + 暂停 + 自动旋转）

| 按钮 | 定位 | 说明 |
|---|---|---|
| **轴测全景** | 默认视角 | 沿隧道轴线斜视，可见管片环缝、双层钢筋网与病害标注，**汇报开场建议用此视角** |
| **隧道内部** | 洞口外侧低视线 | 透视进入隧道净空，观察衬砌内弧面与内部照明灯带 |
| **横断面剖切** | 里程 K3+131 | **自动开启剖切**，呈现"管片—注浆层—围岩"分层与壁后病害赋存关系 |
| **俯视展开** | 正上方 | 纵向展开，查看病害沿里程的分布密度 |
| **拱顶特写** | 998~1006 环 | 拱顶病害集中区（3 处 I 级病害所在区段） |
| **仰拱特写** | 仰拱范围 | 查看仰拱脱空与不密实区 |
| **检测车第一视角** | 车载视点 | **切换到漫游模式**（非固定视角），自动遍历整条隧道，详见第三章 |
| **⏸ 暂停检测** | 作业控制 | **暂停 / 继续检测作业**，详见第三章第 8 节 |
| **自由飞行漫游** | 键盘操控 | **切换到漫游模式**（非固定视角），用键盘自由飞行，详见第四章 |
| **自动旋转** | — | 切换自动环绕旋转；**任何手动拖动都会自动关闭该模式** |

所有**固定视角**切换均为**平滑补间动画**（缓入缓出），适合现场投屏演示。

> **模式互斥**：检测车第一视角、自由飞行漫游、固定视角三者互不兼容。切换到任一固定视角会自动退出当前漫游模式；进入自由飞行会自动退出第一视角，反之亦然。退出漫游时会**反解当前相机姿态**回写为轨道参数，因此视角不会跳变。
>
> **暂停不受视角限制**：暂停只冻结"检测作业"本身，与当前处于哪种视角无关；且暂停期间仍可自由旋转、缩放、切换视角。

---

## 三、检测车第一视角（FPV）★

这是"采集—解译"环节的可视化：**以检测车车载雷达的视点，从隧道起点自动行驶到终点**，沿途实时显示探地雷达采集剖面与病害检出提示。

### 1. 如何进入 / 退出

有三种方式，任选其一：

| 方式 | 操作 |
|---|---|
| **左侧栏按钮** | 点击「**▶ 进入检测车第一视角**」（最显眼，推荐） |
| **顶栏按钮** | 点击「**检测车第一视角**」 |
| **键盘** | 按 **F** 键（再按一次或按 **Esc** 退出） |

进入后：顶栏按钮变为「退出第一视角」，屏幕下方中部弹出**第一视角 HUD 面板**，检测车自动从 **980 环（K3+128）** 出发向终点行驶。

> **注意**：进入第一视角会**自动隐藏车体本体**（相机就在车内，否则车身会挡住视线），并自动关闭自动旋转、清除残留剖切面、让检测车从起点重新开始。退出时恢复车体与轴测视角。

### 2. 第一视角 HUD 面板

| 区域 | 内容 |
|---|---|
| **顶部状态条** | 🔴 **REC** 闪烁指示（正在采集）+ "检测车第一视角 · 探地雷达实时采集" + 天线主频 **GPR 400 MHz** |
| **B-scan 雷达剖面图** | 核心内容，见下方说明 |
| **里程进度条** | 实时进度百分比 + 对应里程标（如 `K3+150`） |
| **四格仪表** | 当前环号 / 作业速度（3.6 m/s）/ 已扫里程（m）/ 主频·时窗（400MHz / 30ns） |
| **底部提示条** | 病害检出提示，见下方说明 |

### 3. B-scan 雷达剖面图怎么读

沿里程展开的雷达剖面（B-scan）：**横向 = 沿隧道里程**（窗口为当前里程前后各 8 m），**竖向 = 双程走时**（0 ~ 30 ns）。

| 图像特征 | 对应含义 |
|---|---|
| **顶部最亮的水平亮线** | **直达波**（发射脉冲直接耦合到接收天线） |
| 其下第一条水平带 | **管片内表面反射** |
| **中部两条宽带 + 竖向条纹** | **双层钢筋网强杂波**——竖向条纹对应环向钢筋周期（约 0.18 m），这正是项目中 **RCAN 网络要抑制的对象** |
| **下部的双曲线弧形** | **病害体的绕射波**，弧顶位置即病害实际埋深，曲率反映等效直径 |
| **中央竖向亮线** | 当前扫描位置（扫描线） |
| **左侧刻度** | `0 ns` / `30 ns` 时窗，及窗口起始环号 |

> **演示价值**：B-scan 上的**双曲线**是探地雷达判读病害的经典标志。可以指着它说明"弧顶越靠上、埋深越浅；弧越宽、尺寸越大"，直观展示 RTM 成像前的原始数据形态。

### 4. 病害检出提示

底部提示条随车辆位置实时联动：

| 状态 | 显示 |
|---|---|
| 距 **I / II 级**病害 3 m 以内 | 红色告警：`⚠ 检出 I 严重　空气空洞　998环 / 3°　2.8 m　置信度 94%` |
| 距其他病害 1.6 m 以内 | 青色提示：`· 近旁回波：管片脱空（III 一般）1002 环　1.2 m` |
| 附近无病害 | `本段未发现异常回波` |

> 该提示条自动过滤掉被左侧筛选器隐藏的病害，与筛选状态保持一致。

### 5. 环号过环提示

车辆每经过一环，屏幕上方中央闪烁显示「**第 1002 环**」，持续约 0.45 秒。这直观表达了"**里程 ↔ 环号**"的映射关系，即项目病害定位的基础。

### 6. 第一视角下的操作

| 操作 | 效果 |
|---|---|
| **左键拖动** | 小幅环视（水平 ±66°，俯仰 ±43°），可看向拱顶或边墙的病害 |
| **自动扫掠** | 默认开启：视线以约 0.9 rad/s 的节奏左右摆动 ±31°，模拟机械臂雷达横向扫描 |
| **空格键** | 暂停 / 恢复自动扫掠（仅画面视角的摆动，与"暂停检测"是两回事） |
| **F / Esc** | 退出第一视角 |

> 第一视角下**病害标注引线与点击选中会暂时关闭**（相机在隧道内部，投影会失真），相关信息改由 HUD 检出提示条呈现。
>
> 遍历一次全程约 **13 秒**（48 m ÷ 3.6 m/s）。到达终点后自动从起点重新开始，可循环演示。

### 7. 多机械臂检测系统（第一视角可见）★

本视角要展示的核心动作是「**机械臂贴壁扫查衬砌**」，因此在第一视角下：

| 部件 | 第一视角下的可见性 | 说明 |
|---|---|---|
| **底盘 / 车架 / 车轮 / 驾驶台** | **自动隐藏** | 相机就在车内，否则会挡住整片视野 |
| **立柱门架 + 3 台机械臂** | **保留可见** | 立柱立在相机前方，机械臂自柱顶向隧道拱顶方向做扫查摆动 |

**整车结构（第三人称视角下可见完整连接）**：

```
        [左臂]      [中臂]      [右臂]        ← 3 台机械臂（腕部 GPR 天线阵）
          │           │           │
      ┌───┴───────────┴───────────┴───┐
      │        柱间横梁（门架）        │        ← 肩关节 = 柱顶
      └───┬───────────┬───────────┬───┘
          │ 立柱       │ 立柱       │ 立柱     ← 自底板竖直向上
      ╲   │           │           │   ╱
       ╲──┴───────────┴───────────┴──╱
      ┌───────────────────────────────┐
      │   底板（车架平台面）           │        ← 纵梁 + 横向加强肋
      └───────────────────────────────┘
         ○        ○        ○        ○          ← 车轮（经支柱/轮轴支座连到纵梁）
```

各部分说明：

| 部件 | 说明 |
|---|---|
| **底板** | 平板车平台面，承载立柱门架与驾驶台 |
| **纵梁 + 横向加强肋** | 位于底板正下方，构成车架 |
| **立柱（3 根）** | 自底板竖直向上，**柱顶即肩关节**，机械臂由此伸出 |
| **柱脚法兰** | 立柱与底板的连接节点 |
| **斜撑** | 自柱脚斜向连到底板，形成三角稳定结构 |
| **柱间横梁** | 两根横梁把三根立柱连成整体门架 |
| **车轮（4 个）** | **轮底恰好落在道床面上**，轮轴贯通两侧，经轮罩支柱与轮轴支座连到纵梁 |
| **尾部配重箱 / 前端防撞梁** | 车体前后端结构 |

每台机械臂为二连杆结构：

| 部件 | 说明 |
|---|---|
| 肩部回转座 | 安装在立柱柱顶 |
| **大臂** | 琥珀色连杆，绕肩关节做抬升 / 下压摆动 |
| **肘关节** | 深色球铰，随动微弯（幅度小于大臂） |
| **小臂** | 自肘部继续前伸 |
| **腕部 GPR 天线阵** | 蓝色天线头 + 深蓝发射/接收单元，是实际贴壁扫查的部件 |
| **雷达脉冲环** | 天线阵端面沿衬砌法向扩散的青色圆环，随收发节拍脉冲 |
| **贴壁接触光斑** | 天线阵与衬砌接触点附近的青色光斑，呼吸式明暗，表示"正在扫查" |
| **臂端工作灯** | 中臂腕部的点光源，随扫查节奏明暗变化 |

**三台臂的布置与扫掠区间**（**天线阵沿衬砌内弧贴壁扫掠**，不是原地摆动）：

| 臂 | 横向位置 x | 臂长 | 扫掠区间（环向角，拱顶 0°） | 初始相位 |
|---|---|---|---|---|
| **左臂** | +0.44 m | 2.75 m | 38° ~ 66°（拱顶左侧） | 0.00 |
| **中臂** | 0 m | 2.75 m | 0° ~ 30°（拱顶） | 0.50 |
| **右臂** | −0.44 m | 2.75 m | 0° ~ 28°（拱顶右侧） | 0.25 |

- 三臂分区互补，合计覆盖 **0° ~ 66°** 的拱部范围，无重叠浪费
- 三臂**相位互不相同**，因此不会同步动作，看起来像在分区并行扫查
- 扫掠频率约 **1 Hz**（一个往返）

**运动学说明（关键）**：臂端轨迹**不是正弦摆动，而是由逆向运动学解算出的贴壁轨迹**，且**靶面不是理想圆形，而是随环号变化的实测断面**。

**（1）逆向运动学**

- 由目标贴壁点反解二连杆的肩角与肘角：`a = −(φ+A)`、`e = +(π−B)`（符号组合经数值穷举验证）
- 运行时**逐帧解算**，因此断面变化能实时反映到关节角

**（2）实测断面模型（椭圆度 + 环缝错台）**

靶面半径不再是常数，而是按 `环号 × 环向角` 查表（48 档环向分辨率）：

| 偏差类型 | 模型 | 量级 |
|---|---|---|
| **椭圆度** | `−oval·cos(2θ)`，拱顶/仰拱略压扁、两侧略外扩 | ±14 mm |
| **环缝错台** | 高斯型局部径向凸起，集中在各环缝处的某一环向位置 | 0 ~ 38 mm |

- 断面数据由**确定性伪随机**生成（同一环号每次刷新结果相同，演示可复现）
- 靶面最大半径 **2.682 m**，始终不超过衬砌内表面 2.70 m
- 臂端半径经**一阶低通滤波**跟踪（时间常数约 0.17 s），过环缝时表现为短促的"顶起/让开"，而不是关节角瞬变
- 臂端位置相对车体**前置 0.35 m**，使"正在过环缝"这个过程能被看到

**（3）实测精度**（全环 × 全弧段穷举 **7320 个采样点**）

| 指标 | 结果 |
|---|---|
| 贴壁最大误差 | **0.000000 m** |
| 天线阵穿出衬砌内表面 | **0.000000 m**（不穿出） |
| 全程可达性 | 所需最大距离 2.6551 m ＜ 臂长上限 2.747 m，**余量 0.092 m** |

> 这意味着如果评委问"天线阵真的贴着管片走吗"，答案是：**是的，且是按实测断面走的**——肩肘关节角由目标贴壁点反解得到，靶面半径随环号与环向角变化，而不是人为摆一个好看的角度。

**HUD 同步显示**：底部 HUD 有两格显示断面实测信息 ——

| 格 | 内容 |
|---|---|
| **当前环椭圆度** | 该环的椭圆度值（mm），如 `+11.8 mm` |
| **本环环缝错台** | 该环缝处的径向错台量（mm），如 `18 mm` |
| **臂端贴壁靶面半径 / 断面状态** | 当前臂端实际靶面半径（如 `2.636 m`）+ 状态标识：`① 正在过环缝` / `② 椭圆度影响中` / `③ 断面平稳` |

以及一格「**机械臂**」显示在岗臂数（`3 臂扫查`）。

> **演示提示**：第一视角下如果想看清机械臂本身，建议先关闭「壁后注浆层」与「城市天际线」两个开关，画面更干净；机械臂与天线阵会更突出。
>
> 想向评委展示"断面不是理想圆"，勾选 **断面偏差可视化**，会叠加逐环彩色线圈（暖色=错台凸出、冷色=椭圆度压扁）；此时 HUD 上的椭圆度/错台数值与彩色线圈可相互印证。

### 8. 随时暂停 / 继续检测 ★

检测作业可以在**任意时刻暂停与继续**，用于讲解时定格画面、或对比暂停前后的 B-scan 剖面。

**三种操作方式**：

| 方式 | 操作 |
|---|---|
| **顶栏按钮** | 点击「**⏸ 暂停检测（P）**」，按钮变为「▶ 继续检测（P）」并高亮 |
| **左侧栏按钮** | 点击「**⏸ 暂停 / 继续检测（P）**」 |
| **键盘** | 按 **P** 键切换 |

**暂停时会冻结的内容**（整体表现为"作业停住"）：

| 冻结项 | 说明 |
|---|---|
| **车辆行进** | 检测车停在当前位置不再前进（第一视角与普视巡游都会停） |
| **机械臂扫查** | 三台臂的贴壁扫掠、肘部随动全部定格（停在当前弧面位置） |
| **天线阵脉冲环 / 衬砌光斑 / 臂端工作灯** | 停止脉冲与呼吸 |
| **B-scan 雷达剖面** | 剖面图内容随车辆位置生成，车停即停（扫描线不再推进） |
| **视角自动扫掠与云台侧倾** | 第一视角的视线摆动停下，画面完全静止 |
| **过环提示** | 不再触发新的"第 N 环"闪烁 |

**暂停时仍然可以操作的内容**（方便定格后细看）：

- 鼠标拖动翻转 / 滚轮缩放 / 右键平移
- 切换任意视角预设、切换漫游模式
- 拖动纵剖切面、勾选显示控制开关
- 点击病害体查看详情

**状态指示**：

- 顶栏按钮高亮并显示「▶ 继续检测（P）」
- 第一视角 HUD 左上角的 **REC** 指示灯由**红色闪烁**变为**黄色常亮**，文字变为「**暂停**」

> **演示用法**：车辆驶近某处病害时按 **P** 定格，此时 B-scan 上正好停在该病害的双曲线位置，可以指着剖面慢慢讲；讲完再按 **P** 继续。
>
> **与空格键的区别**：**P** 暂停的是整个检测作业（含车辆行进）；**空格**只暂停第一视角的视线自动摆动，车仍在前进。

---

## 四、自由飞行漫游（G 键）

用于**不受轨道约束地自由观察**：可飞入隧道内部、绕到结构背后、升到地表俯瞰，适合讲清病害的空间位置关系。

### 1. 如何进入 / 退出

| 方式 | 操作 |
|---|---|
| **左侧栏按钮** | 点击「**🚁 自由飞行漫游（G 键）**」 |
| **顶栏按钮** | 点击「**自由飞行漫游**」 |
| **键盘** | 按 **G** 键 |

进入后：按钮变为「✕ 退出自由飞行（G 键）」并高亮，屏幕底部出现**操作提示条**。再按一次 **G** 即退出。

> 进入时以**当前相机姿态**作为飞行起点与朝向，因此不会出现视角跳变。若此刻正在检测车第一视角，会先自动退出该模式。

### 2. 飞行操作键位

| 按键 | 动作 |
|---|---|
| **W / S** | 前进 / 后退（沿视线方向） |
| **A / D** | 左移 / 右移（水平横移，不受俯仰影响） |
| **E / Q** | 上升 / 下降 |
| **Shift** | 加速（**8 → 24**，即常速 3 倍） |
| **按住左键拖动** | 转动视角（水平 ±86°，俯仰 ±85.9°） |

> 屏幕底部的提示条始终显示这套键位，现场演示时评委也能直接看懂。

### 3. 安全边界

为避免"飞丢"，相机被限制在场地外扩范围内（该范围比隧道与地层模型大一圈，足够绕行观察）：

| 轴向 | 范围 |
|---|---|
| X（沿里程） | −60 ~ 110 |
| Y（竖向） | −16 ~ 80 |
| Z（横向） | −50 ~ 50 |

### 4. 适用场景

- **看病害的空间关系**：飞入隧道内部，绕到某处空洞旁，直观感受它相对拱顶、管片、钢筋网的位置
- **看整体环境**：升到地表以上，结合城市天际线说明隧道在城市中的位置（见第五章"背景环境"）
- **慢速讲解**：不加 Shift 时速度较慢，适合边飞边讲

> 退出后相机会停在当前位置对应的轨道视角上，可直接接着用鼠标拖动继续观察。

---

## 五、界面功能详解

界面分为六个区域：**顶栏**（工点信息与视角）、**左侧栏**（总体评估、病害清单与显示控制）、**中央 3D 视口**、**右侧栏**（病害详情与时序分析）、**底部图例**、**底部漫游 HUD**（仅在检测车第一视角 / 自由飞行时出现）。

### 1. 顶栏 · 工点信息

显示当前工点的演示数据，用于向评委说明数据来源：

- 数据链路状态（呼吸灯表示在线）
- 工点：地铁 4 号线 · 王家湾隧道
- 里程：K3+128 ~ K3+172
- 管片环：980 ~ 1019
- 解译算法链：RCAN + RTM + YOLOv7-AFPN

### 2. 左侧栏 · 结构健康指数 SHI

- **仪表盘**：分段色带（红/橙/黄/浅绿/绿）对应 0~100 分五个区间，白色指针指示当前值。
- **当前结果：SHI = 62.3 / 100，等级 C（较差）**
- 仪表下方显示状态描述、**严重病害（I+II 级）占比 25.0%**、病害密度 0.40 处/环。

**SHI 等级划分**（依据项目计划书 5.6.3）：

| SHI 区间 | 等级 | 状态描述 | 运维策略 |
|---|---|---|---|
| 90 ~ 100 | **A（优）** `#48e0a0` | 结构完好，无明显病害 | 常规巡检（1 年/次） |
| 75 ~ 90 | **B（良）** `#7fdc5a` | 存在轻微病害，不影响使用 | 加强巡检（半年/次） |
| 60 ~ 75 | **C（较差）** `#ffd633` | 存在中等病害，需关注 | 专项整治 + 季度监测 |
| 40 ~ 60 | **D（差）** `#ff9d2e` | 存在较严重病害 | 立即专项检测 + 限期治理 |
| 0 ~ 40 | **E（危险）** `#ff3b3b` | 存在严重病害，安全受威胁 | 限速/停运评估 + 应急处置 |

### 3. 左侧栏 · 病害类型分布

横向条形图统计六类病害数量，条形长度按最大值归一化，颜色与底部图例一致：

| 类型 | 颜色 | 数量 |
|---|---|---|
| 空气空洞 | 红 `#ff2d55` | 5 处 |
| 充水/富水 | 青 `#3fd0ff` | 3 处 |
| 管片脱空 | 橙 `#ffb020` | 2 处 |
| 不密实 | 棕 `#c07b3a` | 3 处 |
| 钢筋异常 | 紫 `#b44cff` | 2 处 |
| 裂缝/渗漏 | 黄 `#ffe14d` | 1 处 |
| **合计** | | **16 处** |

### 4. 左侧栏 · 病害等级筛选

两排可点击的胶囊按钮，**点击即切换该类病害的显隐**（按钮变暗表示已隐藏）：

- 上排按**等级**筛选：I 级(3) / II 级(1) / III 级(6) / IV 级(6)
- 下排按**类型**筛选：六类病害

被隐藏的病害在 3D 场景、HTML 标签、右侧环向展开图中会**同步消失**。若当前选中的病害被隐藏，会自动取消选中。

### 5. 左侧栏 · 病害清单（★ 新增）

等级筛选下方是**全部 16 处病害的清单列表**，每行显示：

| 列 | 内容 |
|---|---|
| 等级色块 | ■ 颜色对应 I~IV 级（红 / 橙 / 黄 / 绿） |
| 编号 | `D-001` ~ `D-016` |
| 类型 | 空气空洞 / 充水富水 / 管片脱空 / 不密实 / 钢筋异常 / 裂缝渗漏 |
| 位置 | 管片环号 + 环向角，如 `998环/3°` |
| **「定位」按钮** | 点击后**选中该病害并飞行聚焦**到它旁边 |

**两种点击方式**（有区别，演示时注意）：

- **点击整行** → 仅**选中**该病害（右侧详情面板刷新、环向展开图高亮），相机不动
- **点击行尾「定位」按钮** → 选中 **并** 让相机飞行聚焦过去

> 与"病害等级筛选"联动：被筛选隐藏的病害不会出现在清单中。清单顺序为台账原始顺序（按编号）。

### 6. 左侧栏 · 显示控制（12 个开关）

| 开关 | 默认 | 作用 |
|---|---|---|
| 管片衬砌（透视） | ✔ 开 | 显示盾构管片环。**关闭后会同时将透明度归零**，便于单独观察壁后病害 |
| 隐蔽病害体（高亮） | ✔ 开 | 显示全部病害本体与光晕。**做病害汇报时若嫌杂乱，可先关闭此项** |
| 钢筋网（双层） | ✔ 开 | 显示双层钢筋网（半径 2.82 m / 2.93 m，环向 40 根/环 + 环向箍筋） |
| 壁后注浆层 | ✔ 开 | 显示壁后注浆层体（外缘半径 3.30 m） |
| 地层模型 | ✔ 开 | 显示五层地层块体与地表栅格（半透明，不影响透视观察隧道） |
| 病害标注引线（仅 I/II 级） | ✔ 开 | 显示 3D 标签与引出线。**默认只标 I/II 级**，避免 16 个标签互相遮挡 |
| 标注全部病害 | ✘ 关 | 勾选后 16 处病害全部显示标签（用于静态截图或导出图） |
| **检测车 / 机械臂组** | ✔ 开 | 显示检测车车身（底板 + 车架 + 车轮 + 驾驶台）与 **3 台机械臂**（立柱门架 + 大臂 + 肘关节 + 小臂 + 腕部 GPR 天线阵）。机械臂沿隧道行进时持续做扫查摆动，天线阵带雷达脉冲环与衬砌扫查光斑。**第一视角下该部件自动分两层处理：底盘隐藏、立柱与机械臂保留可见** |
| GPR 电磁波射线 | ✘ 关 | 显示从车载天线到各病害体的电磁波射线路径（青色为入射路径，黄色为壁后反射界面） |
| **星空背景** | ✔ 开 | 显示 900 点星空（上半球分布，带轻微闪烁）。关闭可让背景纯色，突出隧道本体 |
| **城市天际线** | ✔ 开 | 显示程序化生成的夜景城市楼群（亮窗纹理 + 楼顶障碍灯）。**城市叙事场景建议开启** |
| **断面偏差可视化** | ✘ 关 | 叠加逐环"实测断面"彩色线圈：**暖色=径向凸出（环缝错台），冷色=压扁（椭圆度）**。勾选后可直观看出衬砌断面的实际偏差，详见第三章第 7 节 |

> **纵剖切面滑块**：拖动可在 0~100% 之间沿里程剖开隧道，右侧实时显示对应的里程标（如 `K3+152`）。
> 拖到最右端（100）显示"关"，即恢复完整模型。它可与任意视角叠加使用。

### 7. 左侧栏 · 操作按钮

| 按钮 | 功能 |
|---|---|
| **▶ 进入检测车第一视角** | 进入检测车车载雷达视点，自动遍历整条隧道（详见第三章），进入后按钮变为「退出第一视角」 |
| **⏸ 暂停 / 继续检测（P）** | 随时冻结 / 恢复检测作业（详见第三章第 8 节），按钮文案随状态切换 |
| **🚁 自由飞行漫游（G 键）** | 进入键盘自由飞行模式（详见第四章），进入后变为「✕ 退出自由飞行」 |
| **下一处病害** | 依次切换到下一处可见病害，并自动飞行聚焦到该病害（循环播放） |
| **重置视角** | 回到"轴测全景"默认视角 |
| **剖切总览** | 一键执行"轴测视角 + 沿里程剖开一半"，**最能体现壁后病害赋存关系的展示动作** |
| **预警清单** | 自动筛选出 I/II 级病害（隐藏 III/IV 级），并弹出预警清单对话框，逐条列出编号、类型、环号、环向角、d_eq、埋深、置信度、等级 |
| **导出检测评估报告** | 一键生成检测评估报告初稿并下载 |

### 8. 右侧栏 · 病害详情

初始显示操作提示。**点击 3D 场景中的任一病害体**后，显示该病害的完整量化信息：

- **表头**：等级色块（I/II/III/IV）＋ 病害类型 ＋ 编号 ＋ 等级名称与处置原则
- **八宫格量化指标**：

| 指标 | 说明 |
|---|---|
| 管片环号 RING | 所在管片环 |
| 环向角度 θ | 0~360°，**拱顶为 0°** |
| 等效直径 d_eq | 单位 m（裂缝显示"缝宽 w"mm，钢筋异常显示"间距偏差"%） |
| 埋深 h | 距衬砌内表面深度，单位 m |
| 成像面积 S | RTM 成像分割连通域面积，单位 m² |
| 延伸长度 L | 沿里程方向跨度，单位 m |
| 置信度 C | 0~100%（红色渐变：绿=高置信） |
| 单体评分 | 0~100 分（颜色随分数变化） |

- **图像编号 / 证据链**：对应的 RTM 成像图或图像编号（如 `RTM-998-003`）
- **分级判据**：说明该病害等级的判定依据
- **解译结论**：病害成因与形态描述
- **处治建议**：可直接用于养护派工的建议（如"立即专项复测，限期壁后注浆回填"）
- **按钮**：视角聚焦 / 上一处 / 下一处病害

### 9. 右侧栏 · 环向展开定位图

将隧道**沿环向展开为二维平面**的病害分布图：

- **横轴**：管片环号（980 → 1019）
- **纵轴**：环向角 0°~360°（拱顶 0° → 侧墙 90° → 仰拱 180° → 侧墙 270°）
- 每个色块代表一处病害，**颜色 = 等级**，色块高度表示病害在该环向位置的分布范围
- **点击 3D 中的病害，此图中对应色块会加白色高亮边框**
- 被筛选隐藏的病害在此图中同步变淡

> 这是本项目"**里程 + 环号 + 环向角度**"三位一体定位方法的直观体现，可向评委说明病害如何精确落到"第几环、环内哪个角度"。

### 10. 右侧栏 · SHI 多期次演化

- **折线图**：5 期检测的 SHI 变化（2021-11 至 2025-11），实线为实测，**橙色虚线为预测值**
- 背景色带对应 C/D/E 等级区间
- 图下显示三项结论：

| 指标 | 当前值 |
|---|---|
| 劣化速率 R | **−2.99 /年**（低于 −2/年 阈值，标注"⚠ 触发重点关注"） |
| 风险指数 RI | 0.71（高） |
| 建议复检周期 | 3 个月 / 次 |

### 11. 底部图例

三组图例，向评委说明颜色含义：**病害等级**（I~IV）、**病害类型**（含圆形/菱形区分空洞类与结构异常类）、**结构**（管片衬砌 / 注浆层 / 围岩）。

### 12. 底部漫游 HUD（按需出现）

仅在两种漫游模式下出现，位置见下表：

| 模式 | HUD 形态 | 内容 |
|---|---|---|
| **检测车第一视角** | 屏幕下方中部面板 | B-scan 剖面、里程进度、检出提示等，详见第三章第 2~5 节 |
| **自由飞行漫游** | 屏幕底部一条提示条 | 键位提示：`自由飞行：W/S 前后 · A/D 左右 · Q/E 升降 · Shift 加速 · 按住左键拖动转视角 · G 退出` |

两种 HUD 均随模式进入/退出自动显示/隐藏，不占用普通浏览时的画面空间。

### 13. 背景环境（星空 + 城市天际线）

平台内置程序化生成的背景环境，用于"城市生命线"叙事：

| 元素 | 说明 |
|---|---|
| **星空** | 900 个点分布在上半球，带轻微亮度闪烁，营造夜景氛围 |
| **城市天际线** | 程序化生成的楼群，墙面为 Canvas 绘制的**亮窗纹理**（暖黄 / 冷蓝窗光随机混合），楼顶带红色障碍灯 |

两点设计细节：

- 背景层**不受场景雾影响**，按距离手动降低透明度，形成空气层次感；
- **楼群避开了隧道与地层走廊**，不会与主体模型穿插。

由"显示控制"中的**星空背景** / **城市天际线**两个开关独立控制。做纯技术讲解时建议关闭，画面更干净；做城市 / 生命线叙事时建议开启。

---

## 六、推荐演示流程（现场 3~5 分钟）

按此顺序操作，叙事最连贯：

1. **开场**：打开页面，停在**轴测全景**。说明"这是王家湾隧道 K3+128~K3+172 段 40 环管片的三维孪生模型"。
2. **看总体**：指向左侧 SHI 仪表 —— **SHI = 62.3，C 级（较差）**，劣化速率 −2.99/年，已触发重点关注。
3. **看分布**：拖动鼠标**翻转隧道**，展示病害在拱顶、拱腰、边墙、仰拱的分布；指出底部图例的六类病害配色。
4. **看检测车**：切到**轴测全景**或**拱顶特写**，看检测车全貌 —— "平板车载着**三台机械臂**，立柱门架把机械臂架到拱顶附近，天线阵分区扫查"。可点 **俯视展开** 看三臂横向错开的门架布置。
5. **随车采集**：点击 **▶ 进入检测车第一视角** —— 画面切到车载雷达视点，自动从 980 环驶向终点。指下方 HUD：
   - "这是**车载雷达的实时采集剖面（B-scan）**，横向是里程、竖向是双程走时"；
   - 指着中部的**竖向条纹**："这是**双层钢筋网的强杂波**，它把浅层空洞的回波淹没了——这正是我们要用 **RCAN 网络抑制**的对象"；
   - 车辆驶过 998 环时，提示条变红："**检出 I 严重 空气空洞 998环**" —— "原始数据阶段就已经能报出疑似病害"；
   - 屏幕上方闪烁「第 998 环」——"**里程与环号实时对应**，这是病害定位的基础"。
   - **按 P 暂停**（★ 新增）：车停在原位，B-scan 正好冻结在该病害的双曲线位置。"这样我可以定格下来仔细看这条双曲线——弧顶就是埋深。"讲完再按 P 继续。
   - 讲完按 **Esc** 退出。
6. **剖开看隐蔽**：点击 **剖切总览** —— 隧道沿里程剖开，直接看到"管片—注浆层—围岩"分层，以及**藏在衬砌背后的空洞、脱空、不密实体**。强调"这些都是肉眼和常规巡检看不到的隐蔽病害"。
7. **点选讲细节**：点击拱顶那处**红色空洞（D-001，998 环，d_eq=0.42 m）** —— 右侧弹出八宫格量化指标与处治建议；同时环向展开图中该点高亮，说明"病害精确定位到 998 环、环向 3°"。也可以直接在左侧 **病害清单** 里点 D-001 那一行的 **「定位」**，相机会自动飞过去。
8. **联动预警**：点击 **预警清单** —— 自动筛出 I/II 级病害并弹出清单，说明"系统自动分级，输出可直接用于养护派工的清单"。
9. **给结论**：点击 **导出检测评估报告** —— 下载报告初稿，说明平台可自动生成图文报告，人工复核后签发。
10. **（可选）自由飞行看空间关系**：点击 **🚁 自由飞行漫游**（或按 **G**），用 **W/S** 飞进隧道、**E/Q** 升降、按住左键转视角，绕到某处病害旁说明它相对拱顶 / 管片 / 钢筋网的空间位置。此模式最适合回答"这个空洞到底在哪儿"这类追问。
11. **（可选）讲算法**：勾选 **GPR 电磁波射线**，展示从车载天线到病害体的电磁波传播路径，衔接第 ② 步的 RCAN + RTM 算法链。
12. **（可选）讲城市叙事**：确认 **城市天际线** 与 **星空背景** 已开启，**俯视展开** 或自由飞行升到地表以上，说明隧道在城市生命线中的位置。

> **时间不够时的精简版**（约 2 分钟）：① 开场 → ⑤ 随车采集（讲到钢筋杂波与检出提示即停，用 P 定格一次）→ ⑥ 剖切总览 → ⑦ 点选 D-001（或用清单「定位」）→ ⑧ 预警清单。
>
> 若评委想自己操作：告诉他"**左键拖就是转，滚轮就是缩放，点病害体就出详情；想看车载视角按 F，想自己飞按 G，想定格按 P**"。

---

## 七、附录 A：模型技术参数

### 1. 隧道几何

| 参数 | 取值 |
|---|---|
| 隧道类型 | 盾构隧道 · 单洞单线 |
| 衬砌内半径 Ri | 2.70 m（内径 5.4 m） |
| 衬砌外半径 Ro | 3.05 m（**管片厚 0.35 m**） |
| 壁后注浆层外缘 | 3.30 m |
| 管片环宽 | 1.20 m |
| 环数 | 40 环（980 ~ 1019 环） |
| 模型里程长度 | 48 m |
| 双层钢筋网半径 | 2.82 m / 2.93 m（环向 40 根/环） |
| 地层模型 | 宽 24 m × 高 22 m，五层（上覆土体 / 过渡层 / 强风化岩 / 中风化岩 / 基岩）＋ 地表栅格 |

### 2. 坐标与角度约定

- **X 轴** = 隧道轴线（+X 为里程增加方向）
- **Y 轴** = 竖直向上（**拱顶在 +Y**）
- **Z 轴** = 水平横向
- **环向角 θ**：拱顶 = 0°，沿逆时针增加；**90° = 右侧墙，180° = 仰拱，270° = 左侧墙**
- **埋深 h**：距衬砌内表面的深度（不是到隧道中心轴的距离）

### 3. 病害径向赋存位置

壁后空洞 / 脱空 / 不密实体的中心按"**管片背后注浆层—围岩**界面"对称发育定位：

- 受两道约束：**内缘不侵入衬砌结构、外缘不超出注浆层控制线**
- 规模较大者（如不密实区）会向围岩方向延伸，**这是真实赋存形态**，围岩模型采用半透明处理以保证可透视观察
- 内弧面裂缝为**表面张开型**，贴合管片内弧面，不侵入隧道净空

---

## 八、附录 B：病害台账（16 处）

| 编号 | 类型 | 等级 | 环号 | 环向 θ | d_eq/w/k | 埋深 h | 面积 S | 延伸 L | 置信度 | 单体评分 |
|---|---|---|---|---|---|---|---|---|---|---|
| D-001 | 空气空洞 | **I** | 998 | 3° | 0.42 m | 0.12 m | 0.139 m² | 0.55 m | 96.0% | 30.1 |
| D-002 | 充水/富水 | **I** | 1013 | 121° | 0.38 m | 0.34 m | 0.113 m² | 0.72 m | 91.0% | 66.2 |
| D-003 | 空气空洞 | **I** | 983 | 60° | 0.31 m | 0.09 m | 0.075 m² | 1.15 m | 94.0% | 30.4 |
| D-004 | 充水/富水 | III | 990 | 300° | 0.24 m | 0.22 m | 0.045 m² | 0.30 m | 88.0% | 65.3 |
| D-005 | 空气空洞 | **II** | 1006 | 20° | 0.22 m | 0.15 m | 0.038 m² | 0.25 m | 90.0% | 52.8 |
| D-006 | 空气空洞 | III | 984 | 76° | 0.20 m | 0.44 m | 0.031 m² | 0.28 m | 87.0% | 71.7 |
| D-007 | 管片脱空 | III | 1002 | 35° | 0.19 m | 0.06 m | 0.028 m² | 0.60 m | 86.0% | 58.8 |
| D-008 | 管片脱空 | III | 1015 | 160° | 0.16 m | 0.11 m | 0.020 m² | 0.45 m | 84.0% | 77.6 |
| D-009 | 不密实 | IV | 994 | 250° | 1.29 m | 0.24 m | 1.300 m² | 2.40 m | 83.0% | 81.5 |
| D-010 | 不密实 | IV | 987 | 108° | 0.90 m | 0.20 m | 0.640 m² | 1.10 m | 80.0% | 85.1 |
| D-011 | 不密实 | IV | 1010 | 190° | 0.70 m | 0.22 m | 0.380 m² | 0.80 m | 79.0% | 90.3 |
| D-012 | 充水/富水 | III | 1006 | 322° | 0.13 m | 0.20 m | 0.013 m² | 0.18 m | 81.0% | 78.2 |
| D-013 | 空气空洞 | IV | 998 | 40° | 0.08 m | 0.16 m | 0.005 m² | 0.10 m | 86.0% | 84.6 |
| D-014 | 裂缝/渗漏 | III | 990 | 355° | 2.0 mm | 0.00 m | 0.036 m² | 1.80 m | 77.0% | 60.0 |
| D-015 | 钢筋异常 | IV | 1002 | 135° | 35% | 0.04 m | 0.070 m² | 0.60 m | 75.0% | 88.0 |
| D-016 | 钢筋异常 | IV | 1013 | 239° | 30% | 0.05 m | 0.049 m² | 0.50 m | 72.0% | 87.8 |

> d_eq/w/k 列：空洞/脱空/富水为**等效直径**；裂缝为**缝宽**；钢筋异常为**间距偏差率**。

---

## 九、附录 C：评估模型算法

### 1. 单体评分

```
score_i = 100 × (1 − s_type × s_size × s_depth × s_pos)      ∈ [0, 100]
```

| 因子 | 取值 |
|---|---|
| **类型严重度** s_type | 空洞 0.92 ＞ 富水空洞 0.84 ＞ 脱空 0.74 ＞ 裂缝 0.44 ＞ 不密实 0.48 ＞ 钢筋异常 0.18（越大越严重） |
| **尺寸因子** s_size | 按类型取控制量并归一化：空洞/脱空/富水 → d_eq / 0.30 m；不密实 → 等效直径 / 1.40 m（由成像面积反演）；裂缝 → 缝宽 / 2.20 mm；钢筋异常 → 间距偏差 / 0.32 |
| **埋深因子** s_depth | `1 − h / 0.50`，下限 0.50（越浅越危险，影响剥落风险） |
| **位置因子** s_pos | 拱顶（±40°）1.00 ＞ 拱腰 0.88 ＞ 边墙 0.70 ＞ 仰拱 0.58，再按 `0.35 + 0.65 × posW` 平缓化 |

### 2. 综合指数 SHI

```
SHI = 100 − (Σ wᵢ(100 − scoreᵢ) / Σ wᵢ) × (1 − W_dens) − ρ_ring × 100 × W_dens
```

- 组合赋权系数 **α = 0.5**（AHP 专家权重 + 熵权客观权重，演示版采用等权 `wᵢ = 1`）
- 密度项权重 **W_dens = 0.10**
- 病害密度 `ρ_ring = N / N_ring`，**0.40 处/环视为满扣**（本工点 16 处 / 40 环 = 0.40）
- 当前结果：**SHI = 62.34 → C 级（较差）**

### 3. 病害等级判定（代码单一判据来源）

等级由 `defectGrade()` 函数统一导出，保证"**清单等级 ↔ 评分 ↔ 判据**"三者永不矛盾：

| 病害类型 | 判定规则 |
|---|---|
| **空洞 / 脱空 / 富水** | 严格按计划书 5.6.2 判据，**以等效直径为主控**：<br>• d_eq ≥ 0.30 m → **I 级**<br>• 0.10 m ≤ d_eq < 0.30 m → 评分 < 56 判 **II 级**，否则 **III 级**<br>• d_eq < 0.10 m → **IV 级** |
| **不密实** | 评分 < 45 → II 级；< 75 → III 级；否则 IV 级 |
| **裂缝 / 钢筋异常** | 标准评分分档：< 40 → I 级；< 60 → II 级；< 80 → III 级；否则 IV 级 |

> 依据：行业判定惯例"衬砌背后未回填深度及直径大于 10 厘米即属于有空洞"，并参考《公路隧道养护技术规范》JTG H12、《铁路隧道衬砌质量无损检测规程》TB 10223、《盾构法隧道施工及验收规范》GB 50446。
> **⚠️ 落地前必须核对标准的现行有效版本与条文号，并在说明书"评价依据"中明确列出。**

### 4. 当前评估结论

- **SHI = 62.3，C 级（较差）**
- 病害总数 **16 处**，密度 0.40 处/环
- 严重病害（I + II 级）占比 **25.0%**
- 劣化速率 **R = −2.99 /年**（近三期平均），低于 −2/年阈值，**触发重点关注**
- 建议：I 级病害立即专项复测并限期壁后注浆回填；II 级纳入年度整治；III/IV 级记录归档，6~12 个月复查

---

## 十、附录 D：导出报告说明

点击 **导出检测评估报告** 后，浏览器会下载一个文本文件：

- **文件名**：`王家湾隧道结构健康检测评估报告_YYYY-MM-DD.txt`
- **编码**：UTF-8（含 BOM，Windows 记事本可直接打开不乱码）
- **内容结构**：
  1. 报告头（工点、里程、检测方式、解译算法、生成时间、评估模型）
  2. 一、结构健康指数（SHI、等级、病害总数、密度、严重病害占比、劣化速率）
  3. 二、病害类型分布
  4. 三、病害清单（按环号排序的完整台账表）
  5. 四、处治建议（分等级给出处置原则）
  6. 五、结论
  7. 落款："本报告由'隧雷智检'隧道结构三维数字孪生平台自动生成，供人工复核后签发"

> 报告内容由页面数据**实时计算生成**，修改病害台账后重新导出即自动同步。

---

## 十一、附录 E：二次修改指引

所有数据与参数都集中在 HTML 文件内的 JavaScript 中，用记事本或 VS Code 即可修改。

| 想改什么 | 在哪里改 | 说明 |
|---|---|---|
| 工点名称、里程、环号 | 搜索 `id="hud"` 附近的 `chip` 区块 | 顶栏展示信息 |
| 隧道尺寸（内径/管片厚/环宽） | 搜索 `var PT = {` | `Ri` 内半径、`Ro` 外半径、`segLen` 环宽、`ring0` 起始环号、`nRing` 环数 |
| 病害台账 | 搜索 `var DEFECTS = [` | 每条含 `id/T/L/ring/az/h/d/Lm/S/C`，裂缝加 `w`，钢筋异常加 `k` |
| 病害配色与名称 | 搜索 `var TYPE = {` | 修改 `color` 与 `name` |
| 等级配色与处置原则 | 搜索 `var LEVEL = {` | 修改 `color` 与 `text` |
| 评分公式各因子 | 搜索 `function singleScore(d)` | 类型严重度、尺寸控制量、埋深、位置因子 |
| 等级判定规则 | 搜索 `function defectGrade(d)` | 修改各类型的判定阈值 |
| 密度满扣基准 | 搜索 `function densityScore()` | 默认 0.40 处/环 |
| SHI 历史数据 | 搜索 `var pts = [` | 5 期 SHI 时序，用于演化曲线与劣化速率 |
| 视角预设 | 搜索 `var VIEWS = {` | `target/theta/phi/dist` 四个参数 |
| 剖切预设位置 | 搜索 `var PRESET_CUT = {` | 横断面视角的默认剖切里程 |
| **第一视角车行速度** | 搜索 `speed: 3.6`（在 `var fp = {` 内） | 单位 m/s，默认 3.6；调大则遍历更快 |
| **第一视角相机位置** | 搜索 `var carEye =` | 车体局部坐标 `(x 右, y 上, z 前)`，默认 `(0.10, 0.34, -0.18)`，决定机械臂在画面中的位置 |
| **第一视角初始俯仰** | 搜索 `pitch: 0.17`（`var fp` 内） | 正值向上，用于把隧道净空置于画面中央 |
| **自动扫掠幅度/频率** | 搜索 `0.55 * Math.sin(clock.elapsedTime * 0.9)` | `0.55` 为扫掠幅度(rad)，`0.9` 为频率 |
| **B-scan 显示窗口** | 搜索 `var WIN = 8.0` | 剖面图横向覆盖的里程半宽（m） |
| **B-scan 时窗与双曲线** | 搜索 `function scanRow` 与 `bscanDraw` | 时窗 30 ns、波长 `LAM=0.09`、孔径半长 `NS=0.5` |
| **机械臂数量 / 位置 / 扫掠区间** | 搜索 `var carCfg = {` | `units` 数组每项：`offX` 横向偏移、`len` 臂长、`az0`/`az1` 扫掠起止环向角（度）、`phase` 初始相位；`shoulderY/shoulderZ` 为肩关节在车体局部坐标中的位置；`contactGap` 为天线阵距衬砌内表面的贴合间隙；`scanRate` 为扫掠往返频率 |
| **贴壁运动学（IK）** | 搜索 `function solveArmIK` | 二连杆反解（`a = −(φ+A)`、`e = +(π−B)`，符号组合经数值穷举验证）。**改动此处需重跑贴合误差校验** |
| **断面实测模型** | 搜索 `var TunnelProfile = (function(){` | `AZ_BUCKETS` 环向分辨率、椭圆度幅值（`0.014`）、错台幅值（`0.038`）、错台过渡宽度（`jointSig`）；`prand()` 为确定性伪随机，换种子即换一套断面 |
| **臂端跟踪与前置** | 搜索 `scanLead` 与 `trackRate`（`var carCfg` 内） | `scanLead` 臂端相对车体的前置距离；`trackRate` 靶面半径跟踪速率（越大跟得越紧、过环缝越"硬"） |
| **臂端弧长位置表** | 搜索 `function buildArmTip` 与 `function tipAzAt` | 按弧长积分的等步距位置表，保证沿断面线速度均匀 |
| **断面偏差可视化** | 搜索 `function buildProfileViz` | 逐环彩色线圈；`setHSL(0.58 − 0.58*t, …)` 为冷暖映射，`t` 由偏差归一化 |
| **机械臂零件尺寸** | 搜索 `function buildArm` | 大臂/小臂截面、腕部天线阵尺寸、脉冲环与接触光斑半径 |
| **车体尺寸与连接结构** | 搜索 `var W = 1.30` 附近（`buildCar` 内） | 底板宽度/纵向范围、纵梁与横肋、立柱高度、斜撑角度、车轮半径与轮距 |
| **车轮落点** | 搜索 `var roadY = -(PT.Ri - 0.30)` | 车轮底面所贴的道床面高度，改动后轮子会与道床脱开 |
| **暂停状态与作业时钟** | 搜索 `var carPaused = false` 与 `var inspT = 0` | `carPaused` 为暂停标志；`inspT` 为可冻结的作业时钟，一切"作业动画"都应由它驱动（`tp`）而非渲染时钟 `t` |
| **暂停按钮文案** | 搜索 `function setPaused` | 顶栏 / 左侧栏按钮文案、HUD REC 指示灯的暂停配色 |
| **自由飞行常速 / 加速** | 搜索 `fly.keys['shift'] ? 24 : 8`（在 `flyUpdate` 内） | `24` 为按住 Shift 的速度，`8` 为常速（单位/秒） |
| **自由飞行软边界** | 搜索 `camera.position.x = clamp`（在 `flyUpdate` 内） | 三行分别为 X / Y / Z 的允许范围 |
| **自由飞行转向灵敏度** | 搜索 `fly.yaw  -= (e.clientX - fly.lx) * 0.0038` | `0.0038` 为鼠标灵敏度 |
| **星空点数** | 搜索 `var N = 900`（在 `buildBackground` 内） | 星空粒子数量 |
| **城市楼群** | 搜索 `function buildingTexture` 与 `buildBackground` | 亮窗纹理配色、楼群尺寸与分布 |
| **病害清单行内容** | 搜索 `function initList` | 行内显示字段与「定位」按钮行为 |

> 修改后**建议先复制一份备份**。修改 `DEFECTS` 后请确保每处病害的环号落在 `ring0 ~ ring0 + nRing - 1` 范围内，否则该病害不会显示。
>
> ⚠️ **注意**：`//` 注释后的同一行不要再写代码。该行代码会被注释吞掉，而 `node --check` 之类的语法检查**查不出来**，只能在浏览器里运行才暴露（表现为页面卡在"正在加载三维孪生场景"）。

---

## 十二、已知限制

1. **数据为演示数据**：工点名称、里程、病害台账均为按项目计划书口径构造的演示数据，尚未接入真实 `.dzt` 道集解译结果。接入真实数据时需替换 `DEFECTS` 数组。
2. **三维模型为参数化几何**：隧道与管片由代码参数化生成，非 BIM 或点云重建模型。后续可替换为 glTF / 3D Tiles 格式的实测模型。
3. **报告输出为纯文本**：当前导出 `.txt` 报告，尚未接入 Qwen2.5-VL / InternVL 多模态大模型生成图文并茂的正式报告。
4. **单工点、单期次视图**：目前同时展示一个工点的多期次 SHI 曲线，尚未实现计划书中的"城市地图 → 线路 → 隧道 → 环 → 病害"五级下钻。
5. **无传统表单式输入**：所有交互均通过鼠标与键盘完成（辅助键：**F** 第一视角 / **G** 自由飞行 / **P** 暂停检测 / **空格** 暂停视线扫掠 / **Esc** 退出第一视角；自由飞行内另有 W A S D Q E Shift，见第四章）。平台为单页演示原型，不含数据上传与用户管理功能。
6. **B-scan 为合成剖面**：第一视角中的雷达剖面图是按病害台账参数**程序化合成**的示意剖面（用于展示原始数据形态与双曲线特征），并非真实 `.dzt` 道集回放。接入实测数据后可改为逐道回放真实 A-scan 序列。
7. **背景环境为程序化生成**：星空与城市天际线为代码生成的装饰性背景（楼群为 Canvas 亮窗纹理），不代表真实城市地理信息，仅用于"城市生命线"叙事氛围。
8. **机械臂采用逆向运动学解算，但介质为刚体近似**：三台机械臂的关节角由二连杆**逆向运动学**反解得到，且靶面为**随环号与环向角变化的实测断面**，天线阵确实沿实测断面贴壁移动。但仍属**刚体运动学近似**——未考虑关节限位、速度/加速度约束、奇异位形规避与碰撞检测。
9. **断面偏差为构造数据，且不改变衬砌的几何显示**：椭圆度（±14 mm）与环缝错台（0~38 mm）按真实盾构隧道的量级与形态**构造**（确定性伪随机，可复现），用于驱动机械臂的贴壁靶面与断面偏差可视化。衬砌**几何显示仍按名义圆形**绘制（设计断面），实测偏差只作用于作业靶面——因此彩色线圈与管片网格之间会有毫厘级的错开，这是有意为之，用来区分"设计断面"与"实测断面"。
10. **暂停不保存进度**：暂停只冻结当前画面与作业状态，刷新页面后检测车会回到出发点重新开始，不做断点续检。

---

*文档版本 v1.6 ｜ 对应平台文件：`隧道三维数字孪生平台.html`（three.js r160）*
*v1.1 更新：新增第三章「检测车第一视角（FPV）」；演示流程新增第 4 步；二次修改指引新增第一视角相关参数；已知限制补充 B-scan 为合成剖面说明。*
*v1.2 更新：同步合作者提交（`419acd1` / `753a376`）——新增第四章「自由飞行漫游（G 键）」、第五章第 5 节「病害清单」、第 12 节「底部漫游 HUD」、第 13 节「背景环境（星空 + 城市天际线）」；显示控制由 9 个开关更新为 11 个；视角预设补充两个漫游模式按钮与模式互斥说明；演示流程新增自由飞行与城市叙事两步；章节顺延至十二章。*
*v1.3 更新：检测车由单臂改为 **3 台机械臂 GPR 检测系统**；第一视角改为「仅隐藏底盘、保留机械臂可见」，使"机械臂贴壁扫查"在车载视点中直接可见；新增第三章第 7 节「多机械臂检测系统」；HUD 增加「机械臂」臂数显示；显示控制说明与二次修改指引同步更新。*
*v1.4 更新：① **重做检测车建模** —— 改为平板车结构（底板 + 纵梁 + 横肋 + 车轮 + 驾驶台），机械臂经**立柱门架**（立柱 + 柱脚法兰 + 斜撑 + 柱间横梁）与车体连成一体，修复第三人称视角下"机械臂与车体分离悬空"的问题；车轮落点改为贴合道床面并经支柱/轮轴支座与纵梁相连。② **新增「随时暂停/继续检测」功能** —— 顶栏按钮 / 左侧栏按钮 / 键盘 **P** 三种方式，暂停时冻结车辆行进、机械臂扫查、天线脉冲、扫查光斑、B-scan 扫描线、视线扫掠与过环提示；HUD 的 REC 指示转为黄色「暂停」。新增第三章第 8 节说明；演示流程增加暂停定格用法；已知限制补充机械臂为视觉动作、暂停不保存进度两条。*
*v1.5 更新：**机械臂改为逆向运动学（IK）贴壁扫掠** —— 原来臂端做正弦摆动（仅视觉动作），现改为沿衬砌内弧逐点反解二连杆关节角，预生成 49 个位姿的解序列，运行时三角波往返插值；实测贴合误差 0.00000 m，腕部半径全程恒为 2.640 m。臂长由 0.72 m 增至 2.66 m（按"肩关节到内弧最远距离"反算，保证全程可及），相机视点相应抬高至车体上方 (0, 1.60, −0.45) 以俯视观察机械臂作业。第三章第 7 节新增扫掠区间表与运动学说明；二次修改指引更新为 IK 相关条目；已知限制第 8 条改写为"采用 IK 但为刚体近似"。*
*v1.6 更新：**贴壁靶面由理想圆形改为随环号变化的实测断面** —— 新增 `TunnelProfile` 断面模型：① 椭圆度（`−oval·cos2θ`，±14 mm，拱顶/仰拱压扁）；② 环缝错台（高斯型局部径向凸起，0~38 mm），按 `环号 × 环向角` 预计算 48 档 LUT，运行时二维插值。机械臂改为**逐帧实时 IK 解算**（不再查预生成序列），靶面半径随环号与环向角变化，臂端经一阶低通滤波跟踪（时间常数 0.17 s）以平滑过环缝；臂端相对车体前置 0.35 m 使"过环缝"可见。臂长由 2.66 m 增至 2.75 m（实测全程所需最大距离 2.6551 m，余量 0.092 m）。全环 × 全弧段 7320 点复核：贴壁误差 0.000000 m、不穿出内表面。新增「断面偏差可视化」开关（逐环彩色线圈，暖色=错台、冷色=椭圆度）；HUD 新增椭圆度 / 错台 / 靶面半径与断面状态三格；显示控制由 11 个开关增至 12 个。已知限制第 8、9 条改写，说明断面偏差为构造数据且不改变衬砌几何显示。*
